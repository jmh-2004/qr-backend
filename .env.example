# Postgres connection
DATABASE_URL=postgresql://qruser:qrpass@localhost:5432/qrdb

# Base URL used when backend returns qr_url_full
BASE_URL=http://localhost:8000

# Auth mode:
# DEV = use X-Debug-Uid header (for testing)
# FIREBASE = verify Authorization Bearer token with Firebase
AUTH_MODE=DEV

# Only needed for FIREBASE mode
# FIREBASE_SERVICE_ACCOUNT=./firebase_service_account.json

# Money payout per valid completion (placeholder until ad network)
PAYOUT_PER_VALID_SCAN_USD=0.001
